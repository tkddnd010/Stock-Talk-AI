{
  "name": "stock-talk-ai",
  "version": "0.0.1",
  "private": true,
  "description": "Stock-Talk-AI monorepo root",
  "scripts": {
    "backend:build": "npm run build --prefix backend",
    "backend:start": "npm run start --prefix backend",
    "backend:start:dev": "npm run start:dev --prefix backend",
    "backend:lint": "npm run lint --prefix backend",
    "backend:test": "npm run test --prefix backend"
  }
}
